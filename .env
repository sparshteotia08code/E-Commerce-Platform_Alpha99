# Server Configuration
PORT=5000
NODE_ENV=development

# Database Settings (PostgreSQL or SQLite fallback)
DB_DIALECT=postgres
DB_HOST=localhost
DB_USER=alpha_user
DB_PASSWORD=alpha_password
DB_NAME=alpha_db
DB_PORT=5432

# Caching & Rate Limiting (Redis or Memory fallback)
REDIS_URL=redis://localhost:6379

# Authentication
JWT_SECRET=super_secret_alpha_key_99

# Client URL
FRONTEND_URL=http://localhost:3000

# Stripe Payment Keys (mock active in dev if empty)
STRIPE_SECRET_KEY=sk_test_placeholder
STRIPE_WEBHOOK_SECRET=whsec_placeholder

# Transactional Email (mock active in dev if empty)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=user_placeholder
SMTP_PASS=pass_placeholder
SMTP_FROM=noreply@alpha99.com
